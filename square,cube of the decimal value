#include <stdio.h>
int main() {
  float num;
  printf("Enter a decimal number: ");
  scanf("%f", &num);
  printf("Square of %.2f: %.2f\n", num, num * num);
  printf("Cube of %.2f: %.2f\n", num, num * num * num);
  return 0;
}
